name: latres
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  # Tambahkan 3 baris ini:
  http: ^1.2.0             # Untuk akses API
  provider: ^6.1.1         # Untuk state management
  shared_preferences: ^2.2.2 # Untuk local storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true